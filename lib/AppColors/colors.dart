import 'dart:ui';

class UberColor {
  // Primary Colors
  static Color uberBlack =
      const Color(0xFF000000); // Black for primary text, background, and icons
  static Color uberGray = const Color(
      0xFF515151); // Gray for secondary text, icons, and subtle elements

  // Secondary Colors
  static Color uberWhite =
      const Color(0xFFFFFFFF); // White for backgrounds, text, and buttons
  static Color uberLightGray =
      const Color(0xFFEDEDED); // Light Gray for borders, background areas
  static Color uberDarkGray =
      const Color(0xFF1C1C1C); // Dark Gray for cards and button backgrounds
  static Color ubernewone = const Color(0XffD3D3D3);
  // Accent Colors
  static Color uberGreen =
      const Color(0xFF1FBF65); // Green for success, confirmations, promotions
  static Color uberRed =
      const Color(0xFFDE3C10); // Red for errors, alerts, and negative actions

  // Background Colors
  static Color backgroundLight =
      const Color(0xFFF6F6F6); // Light background for the app's main areas
  static Color cardBackground =
      const Color(0xFFFFFFFF); // White background for card components

  // Divider and Border Colors
  static Color borderColor =
      const Color(0xFFBEBEBE); // Light Gray for borders and dividers
}
